# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# AI — Groq FREE (llama-3.3-70b-versatile, llama3-8b-8192)
# Get key at: https://console.groq.com (free, no card needed)
groq>=0.9.0
langchain>=0.2.0
langchain-community>=0.2.0

# Local embeddings — FREE, no API key, runs on CPU
# HuggingFace all-MiniLM-L6-v2 downloaded once to ~/.cache
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# ML models (all free, train locally)
numpy>=1.26.0
scikit-learn>=1.4.0
imbalanced-learn>=0.12.0   # SMOTE — free
optuna>=3.6.0               # Optuna hyperparameter tuning — free
xgboost>=2.0.0              # XGBoost — free
lightgbm>=4.3.0             # LightGBM — free
shap>=0.45.0                # SHAP explainability — free
joblib>=1.3.0

# NLP — spaCy en_core_web_sm downloaded via: python -m spacy download en_core_web_sm
spacy>=3.7.0

# Cache — Redis optional (free via Upstash 10k/day or local Docker)
# Falls back to in-memory if REDIS_URL not set — zero setup needed
redis>=5.0.0

# HTTP (live data: World Bank GDP, ExchangeRate-API — both free, no key)
httpx>=0.27.0
python-dotenv>=1.0.0

# Streamlit demo (free)
streamlit>=1.35.0
plotly>=5.22.0
